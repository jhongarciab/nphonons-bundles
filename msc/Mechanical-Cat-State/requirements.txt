# Requires Python 3.11 (QuTiP 4.7.6 does not build cleanly on 3.13).
# QuTiP4 is required (not QuTiP5) because the original paper scripts
# (fig1-5_git_v*.py) use qutip.Options(), removed in QuTiP5; every
# validacion/ script was built against QuTiP4 for direct comparability.
# Cython must stay <3.0 -- QuTiP4's JIT compilation is incompatible
# with Cython>=3. See validacion/HANDOFF.md for full setup steps.
#
# Setup:
#   python3.11 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
contourpy==1.3.3
cycler==0.12.1
Cython==0.29.37
fonttools==4.65.0
kiwisolver==1.5.1
matplotlib==3.11.2
numpy==1.26.4
packaging==26.3
pillow==12.3.0
pyparsing==3.3.3
python-dateutil==2.9.0.post0
qutip==4.7.6
scipy==1.12.0
six==1.17.0
