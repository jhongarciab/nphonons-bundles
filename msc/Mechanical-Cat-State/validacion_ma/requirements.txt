# Entorno QuTiP 5 para validacion_ma/ (el venv de ../validacion usa QuTiP 4.7.6: no mezclar).
# Setup (Python 3.11):
#   uv venv --python 3.11 .venv && uv pip install --python .venv/bin/python -r requirements.txt
# Los coeficientes string de QuTiP 5 se compilan con Cython: requiere Cython, setuptools y un compilador C (gcc).
qutip==5.3.1
numpy
scipy
matplotlib
cython
setuptools
filelock
packaging
